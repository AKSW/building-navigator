
var config = {
    development: {
        store: {
            host: 'http://localhost',
            port: 8890,
            path: '/sparql/'
        },
    },
    production: {
        store: {
            host: 'http://localhost',
            port: 8890,
            path: '/sparql/'
        },
    }
};

module.exports = config;